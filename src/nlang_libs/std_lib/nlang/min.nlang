export def min(a) -> int {
    store n = len(a);
    if (n == 0) {
        return 0;
    }
    @mut store m = a[0];
    for (@mut store i = 1; i < n; i = i + 1) {
        if (a[i] < m) { m = a[i]; }
    }
    return m;
}