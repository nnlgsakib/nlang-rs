export def reverse(a) {
    @mut store i = 0;
    @mut store j = len(a) - 1;
    while (i < j) {
        @mut store tmp = a[i];
        a[i] = a[j];
        a[j] = tmp;
        i = i + 1;
        j = j - 1;
    }
    return a;
}