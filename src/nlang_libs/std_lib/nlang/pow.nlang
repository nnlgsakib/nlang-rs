export def pow(base: int, exp: int) -> int {
    if (exp < 0) { return 0; }
    @mut store result = 1;
    for (@mut store i = 0; i < exp; i = i + 1) {
        result = result * base;
    }
    return result;
}