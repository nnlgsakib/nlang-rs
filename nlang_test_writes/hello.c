#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

void main(void);
void calculate(long long a, long long b);

void main(void) {
    long long x = 10LL;
    long long y = 20LL;
    long long sum = (x + y);
    if ((sum > 25LL)) {
        {
        }
    } else {
        {
        }
    }
    long long result = calculate(x, y);
}

void calculate(long long a, long long b) {
    return (a * b);
}

