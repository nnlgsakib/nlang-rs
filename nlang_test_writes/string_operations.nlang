// Test string operations and handling
def main() {
    // Basic string variables
    store greeting = "Hello";
    store name = "World";
    
    println("Basic strings:");
    println(greeting);
    println(name);
    
    // Test string concatenation (if supported)
    println("Testing string concatenation:");
    // This might not be supported yet
    // store combined = greeting + " " + name;
    // println(combined);
    
    // Test empty strings
    store empty = "";
    println("Empty string:");
    println(empty);
    
    // Test strings with special characters
    store special = "Special chars: !@#$%^&*()";
    println("Special characters:");
    println(special);
    
    // Test multiline-like strings
    store multiline = "Line 1\nLine 2\nLine 3";
    println("Multiline string:");
    println(multiline);
}